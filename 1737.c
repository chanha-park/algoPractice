#include <stdio.h>

long long	arr[1000][320];

long long	g(int a, int b)
{
	if (b == 0)
		return (f(a));
	if (a == 0)
		return (
}

long long	f(float n)
{
}

int	main(void)
{
	float	n;

	scanf("%f", &n);
	printf("%lld\n", f(n));
	return (0);
}

n * M_PI;

(n - 1) M_PI + (M_PI - 1); (n  - 1) M_PI;
